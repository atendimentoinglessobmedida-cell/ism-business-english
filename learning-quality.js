(()=>{'use strict';function mount(){if(document.getElementById('offline-status'))return;const el=document.createElement('div');el.id='offline-status';el.className='offline-status';el.setAttribute('role','status');el.setAttribute('aria-live','polite');document.body.append(el);const update=()=>{const off=!navigator.onLine;el.hidden=!off;el.textContent=off?'Modo offline · conteúdo já carregado continua disponível':'';document.documentElement.toggleAttribute('data-offline',off)};addEventListener('online',update);addEventListener('offline',update);update()}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',mount);else mount();})();